{
  "name": "laptop-api",
  "version": "1.0.0",
  "description": "Simple JSON server for laptop catalog",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch db.json --port 4000 --host 0.0.0.0 --middlewares ./cors.js"
  },
  "dependencies": {
    "json-server": "^0.17.3"
  }
}
